import React from "react";
// import { useAppStore } from "../../store";
import { Layout } from "../../components/Layout";

export function RegisterPage() {
  // const [state] = useAppStore();
  // const { } = state;

  return <Layout>Register Page</Layout>;
}
