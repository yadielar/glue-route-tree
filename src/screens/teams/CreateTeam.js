import React from "react";
// import { useAppStore } from "../../store";
import { Layout } from "../../components/Layout";

export function CreateTeam() {
  // const [state] = useAppStore();
  // const { } = state;

  return <Layout>Create Team</Layout>;
}
