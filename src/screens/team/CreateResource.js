import React from "react";
// import { useAppStore } from "../../store";
import { Layout } from "../../components/Layout";

export function CreateResource() {
  // const [state] = useAppStore();
  // const { } = state;

  return (
    <Layout>
      <h2>Glue to Channel</h2>
    </Layout>
  );
}
