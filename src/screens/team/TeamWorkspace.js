import React from "react";
// import { useAppStore } from "../../store";
import { Layout } from "../../components/Layout";

export function TeamWorkspace() {
  // const [state] = useAppStore();
  // const { } = state;

  return <Layout>Team Workspace</Layout>;
}
